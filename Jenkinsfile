pipeline {
    agent any

    stages {
        stage("Build") {
            echo "Build successed!"
        }

        stage("Test") {
            echo "Test successed!"
        }

        stage("Deploy") {
            echo "Deploy successed!"
        }
    } 
}
